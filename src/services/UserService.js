class UserService {
    create = (dataUser) =>{
        try {
            //Xu lý các nghiệp vụ liên quan
            //gọi đến tầng model
            return true;
        } catch (error) {
            throw error
        }
    }
}
module.exports = new UserService();
